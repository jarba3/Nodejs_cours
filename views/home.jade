extends layout

block content
    h1 Home page
    each article in articles
        article
            h2 #{article.title} <small>écrit le : #{dateFormat(article.created, "dd/mm/yyyy")}</small>
            hr
            p= article.teaser